# `assets/` — Design & Brand Assets

App icons, splash images, illustrations, fonts, and Lottie animations referenced by
the Flutter client. Bundled via `flutter`'s `assets:` declaration in `pubspec.yaml`.

Planned subfolders: `icons/`, `images/`, `fonts/`, `animations/`.

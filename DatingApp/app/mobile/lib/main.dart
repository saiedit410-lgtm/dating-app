import 'package:dating_app/bootstrap.dart';

/// Application entrypoint.
///
/// All real startup wiring (error handling, logging, dependency container,
/// and the root widget) lives in [bootstrap] so this stays a single line.
void main() => bootstrap();

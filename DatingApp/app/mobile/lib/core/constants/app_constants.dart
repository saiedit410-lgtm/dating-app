/// Static, environment-independent application constants.
abstract final class AppConstants {
  /// Technical package/log name (stable, not user-facing).
  static const String appTechName = 'dating_app';

  /// Default user-facing product name (per-environment name lives in EnvConfig).
  static const String appDisplayName = 'Spark';
}
